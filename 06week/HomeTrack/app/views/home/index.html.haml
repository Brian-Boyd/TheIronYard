%h1 Locations
%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @locations.each do |location|
      %tr
        %td= location.name
        %td= link_to 'Show', location # if can? :read, Location
        %td= link_to 'Edit', edit_location_path(location) # if can? :update, Location
        %td= link_to 'Destroy', location, method: :delete, data: { confirm: 'Are you sure?' } # if can? :destroy, Location
%p
= link_to 'New Location', new_location_path # if can? :create, Location
%p
/ = link_to 'New Assignment', new_assignment_path
%p
/ = link_to 'New Comment', new_comment_path
%p
/ = link_to 'New Course', new_course_path
%p
/ = link_to 'New Submission', new_submission_path
%p
/ = link_to 'New User', new_user_path
